<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container row">  
        <div class="col-sm-6">
            <h1 class="mb-4">Nama Anggota Keluarga :
            </h1>

            <h4> Hubungan Keluarga </h4>
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>Nama Anggota Keluarga</th>
                    <th>Hubungan</th>
                    <th>...</th>
                </tr>
                </thead>
                <tbody>
                    <% for(var i=0; i < identitas_reveal.length; i++) { %>
                        <tr>
                            <td> <%= identitas_reveal[i][0].nama %></td>
                            <td> <%= jenis_relasi[i][0].jenis_relasi %></td>
                            <td>
                                <a href="edit/<%=identitas[i].relasi.id %>" class="btn btn-info">Edit</a>
                                <form action="delete/<%=identitas[i].relasi.id%>?_method=DELETE" method="POST" class="d-inline" >
                                    <input type="hidden" name="id_relasi" value="<%= identitas[0].id %>"/>
                                    <button type="submit" class="btn btn-danger" >DELETE</button>
                                </form>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>

            <a href="new/<%=identitas_tunggal[0].id %>" class="btn btn-primary">Tambah Relasi Kelurga</a>
        </div>

        <div class="col-sm-6">
            <h1 class="mb-4">
                <%= identitas_tunggal[0].nama %>
            </h1>
            <h4> Aset Kekayaan</h4>
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>Aset</th>
                    <th>...</th>
                </tr>
                </thead>
                <tbody>
                    <% for(var i=0; i < aset.length; i++) { %>
                        <tr>
                            <td> <%= aset[i].nama_aset %></td>
                            <td>
                            
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>

            <a href="/kepemilikan_aset/new/<%=identitas_tunggal[0].id %>" class="btn btn-primary">Tambah Aset Anggota Kelurga</a>
        </div>
        </div>
    </div>

</body>
</html>